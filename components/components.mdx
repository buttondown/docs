export const Cover = ({ url }) => {
  return (
    <div
      className="flex w-full h-[180px] sm:h-[220px] rounded-lg mb-8 bg-neutral-50 border border-neutral-100 items-center justify-center overflow-hidden bg-cover bg-center"
      style={{ backgroundImage: `url(${url})` }}
    />
  )
}

<Cover url="https://tinyurl.com/3m36recy" />

export const Card = ({ title, text, blue, green, yellow, href, Icon }) => {
  return (
    <a
      className="group no-underline"
      href={href}
      target="_blank"
      rel="noreferrer"
    >
      <div
        className={`flex h-32 items-center justify-center rounded-md mb-6 group-hover:opacity-80 group-hover:shadow-lg transition transform group-hover:-translate-y-px duration-500 ${
          blue && "bg-gradient-to-r from-sky-400 to-blue-400"
        } ${green && "bg-gradient-to-r from-green-400 to-teal-400"} ${
          yellow && "bg-gradient-to-r from-orange-400 to-amber-400"
        }`}
      >
        <Icon className="text-white w-12 h-12" />
      </div>
      <p
        className={`text-xs uppercase font-semibold mb-2 ${
          blue ? "text-sky-500" : ""
        } ${green ? "text-teal-500" : ""} ${yellow ? "text-amber-500" : ""}`}
      >
        {title}
      </p>
      <p className="text-sm text-neutral-500 p-0 m-0">{text}</p>
    </a>
  )
}

export const LearningResources = () => {
  return (
    <div className="grid grid-cols-1 xs:grid-cols-3 gap-8 xs:gap-4">
      <Card
        title="Documentation"
        text="In-depth tutorials and references."
        blue
        Icon={BookIcon}
        href="https://motif.land/docs"
      />
      <Card
        title="Playground"
        text="Learn and explore in an interactive playground."
        green
        Icon={CodeIcon}
        href="https://motif.land/play?demo=welcome"
      />
      <Card
        title="Changelog"
        text="New features, fixes and improvements."
        yellow
        Icon={NewsIcon}
        href="https://motif.land/changelog"
      />
    </div>
  )
}

<LearningResources />

export const BookIcon = ({ className }) => {
  return (
    <svg
      className={className}
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
    >
      <path
        strokeLinecap="round"
        strokeLinejoin="round"
        strokeWidth={2}
        d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253"
      />
    </svg>
  )
}

export const CodeIcon = ({ className }) => {
  return (
    <svg
      className={className}
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
    >
      <path
        strokeLinecap="round"
        strokeLinejoin="round"
        strokeWidth={2}
        d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"
      />
    </svg>
  )
}

export const NewsIcon = ({ className }) => {
  return (
    <svg
      className={className}
      fill="none"
      viewBox="0 0 24 24"
      stroke="currentColor"
    >
      <path
        strokeLinecap="round"
        strokeLinejoin="round"
        strokeWidth={2}
        d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z"
      />
    </svg>
  )
}