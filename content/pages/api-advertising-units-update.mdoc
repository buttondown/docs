---
title: Updating an advertising unit
endpoint: /advertising_units/{id}
method: patch
---

Update an advertising unit's properties. Pass a new list of `dates` to add or remove slots — dates that aren't in the new list will have their corresponding slots deleted, and new dates will have new slots opened.
